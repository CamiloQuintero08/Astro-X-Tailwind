---
import DocsLayout from "../../../layouts/DocsLayout.astro";

const icons = {
  layout: `<svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 5a1 1 0 011-1h14a1 1 0 011 1v2a1 1 0 01-1 1H5a1 1 0 01-1-1V5zM4 13a1 1 0 011-1h6a1 1 0 011 1v6a1 1 0 01-1 1H5a1 1 0 01-1-1v-6zM16 13a1 1 0 011-1h2a1 1 0 011 1v6a1 1 0 01-1 1h-2a1 1 0 01-1-1v-6z"/></svg>`,
  code: `<svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4"/></svg>`,
  ts: `<svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"/></svg>`,
  layers: `<svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"/></svg>`,
  warning: `<svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"/></svg>`,
};
---

<DocsLayout>
  <div class="max-w-5xl mx-auto px-4 py-10 sm:px-6 lg:px-8 text-white">
    <header
      class="text-center mb-12 p-8 bg-linear-to-r from-cyan-950/40 to-blue-950/40 rounded-xl border border-cyan-500/20 backdrop-blur-sm"
    >
      <div
        class="flex items-center gap-2 text-cyan-400 mb-4 text-sm font-mono tracking-wider"
      >
        <span>DOCS</span>
        <span>/</span>
        <span>CORE CONCEPTS</span>
      </div>
      <h1
        class="text-4xl md:text-6xl font-bold text-white mb-6 bg-clip-text bg-linear-to-r from-white via-slate-200 to-slate-400"
      >
        Layouts & Structure
      </h1>
      <p class="text-xl text-slate-400 max-w-3xl leading-relaxed">
        Layouts are the backbone of your Astro project. They allow you to share
        reusable UI (headers, footers, sidebars) and keep navigation state
        consistent across pages.
      </p>
    </header>

    <section class="mb-24 grid lg:grid-cols-2 gap-12 items-center">
      <div>
        <h2 class="text-2xl font-bold text-white mb-4 flex items-center gap-2">
          <span class="text-cyan-400" set:html={icons.layout} />
          What is it exactly?
        </h2>
        <p class="text-slate-400 mb-6">
          A layout in Astro is simply a <code
            class="text-cyan-300 bg-cyan-950/30 px-1 rounded border border-cyan-900"
            >.astro</code
          > component that acts as a wrapper. Its real superpower is the <strong
            >&lt;slot /&gt;</strong
          > element.
        </p>

        <div class="space-y-4">
          <div
            class="flex items-start gap-4 p-4 rounded-lg bg-slate-900/50 border border-slate-800"
          >
            <div class="bg-blue-500/10 p-2 rounded text-blue-400 mt-1">1</div>
            <div>
              <strong class="text-blue-300 block mb-1">Content Injection</strong
              >
              <span class="text-sm text-slate-400">
                Everything you place inside the layout tags will be teleported
                to wherever the <code>&lt;slot /&gt;</code> is defined.
              </span>
            </div>
          </div>

          <div
            class="flex items-start gap-4 p-4 rounded-lg bg-slate-900/50 border border-slate-800"
          >
            <div class="bg-purple-500/10 p-2 rounded text-purple-400 mt-1">
              2
            </div>
            <div>
              <strong class="text-purple-300 block mb-1"
                >Complete HTML Structure</strong
              >
              <span class="text-sm text-slate-400">
                Layouts usually contain the <code>&lt;html&gt;</code>, <code
                  >&lt;head&gt;</code
                >, and <code>&lt;body&gt;</code> elements to avoid repeating them
                on every page.
              </span>
            </div>
          </div>
        </div>
      </div>

      <div
        class="bg-[#0D1117] rounded-xl border border-slate-800 shadow-2xl overflow-hidden relative group"
      >
        <div
          class="absolute inset-0 bg-linear-to-tr from-cyan-500/5 to-purple-500/5 opacity-0 group-hover:opacity-100 transition-opacity"
        >
        </div>

        <div
          class="flex items-center justify-between px-4 py-3 bg-[#161b22] border-b border-slate-800"
        >
          <div class="flex gap-1.5">
            <div
              class="w-3 h-3 rounded-full bg-red-500/20 border border-red-500/50"
            >
            </div>
            <div
              class="w-3 h-3 rounded-full bg-yellow-500/20 border border-yellow-500/50"
            >
            </div>
            <div
              class="w-3 h-3 rounded-full bg-green-500/20 border border-green-500/50"
            >
            </div>
          </div>
          <span class="text-xs text-slate-500 font-mono"
            >src/layouts/BaseLayout.astro</span
          >
        </div>

        <div class="p-6 font-mono text-sm leading-6">
          <pre><code class="language-astro">{`---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

&lt;html&gt;
  &lt;head&gt;
    &lt;title&gt;{title}&lt;/title&gt;
  &lt;/head&gt;
  &lt;body&gt;
    &lt;Nav /&gt;

    &lt;!-- THE MAGIC SLOT --&gt;
    &lt;slot /&gt;

    &lt;Footer /&gt;
  &lt;/body&gt;
&lt;/html&gt;`}</code></pre>
        </div>
      </div>
    </section>

    <hr class="border-slate-800 mb-20" />

    <section class="mb-24">
      <div class="flex items-center justify-between mb-8">
        <h2 class="text-3xl font-bold text-white">Named Slots</h2>
        <span
          class="bg-cyan-900/30 text-cyan-300 text-xs px-3 py-1 rounded-full border border-cyan-500/30 font-mono"
        >
          Advanced Pattern
        </span>
      </div>

      <p class="text-slate-400 mb-8 max-w-4xl">
        What if you need to inject content into different areas of the layout,
        such as a button in the header and the main content in the body? Use the <code
          >name</code
        > attribute.
      </p>

      <div class="grid md:grid-cols-2 gap-8">
        <div class="bg-[#111] p-6 rounded-xl border border-slate-800">
          <h3
            class="text-slate-200 font-semibold mb-4 border-b border-slate-800 pb-2"
          >
            In the Layout
          </h3>
          <pre
            class="text-sm font-mono text-slate-300 overflow-x-auto"><code>&lt;header&gt;
  &lt;slot name="actions" /&gt;
&lt;/header&gt;

&lt;main&gt;
  &lt;slot /&gt;
&lt;/main&gt;</code></pre>
        </div>

        <div class="bg-[#111] p-6 rounded-xl border border-slate-800">
          <h3
            class="text-slate-200 font-semibold mb-4 border-b border-slate-800 pb-2"
          >
            In the Page
          </h3>
          <pre
            class="text-sm font-mono text-slate-300 overflow-x-auto"><code>&lt;Layout&gt;
  &lt;button slot="actions"&gt;Login&lt;/button&gt;

  &lt;h1&gt;My Page&lt;/h1&gt;
&lt;/Layout&gt;</code></pre>
        </div>
      </div>

      <div
        class="mt-6 p-4 bg-yellow-950/20 border border-yellow-600/20 rounded-lg flex gap-3"
      >
        <span class="text-yellow-500 pt-1" set:html={icons.warning} />
        <div>
          <h4 class="text-yellow-400 font-bold text-sm">Fallback Content</h4>
          <p class="text-sm text-yellow-200/70 mt-1">
            You can define default content inside <code
              >&lt;slot&gt;Fallback&lt;/slot&gt;</code
            >. This will only render if no content is passed from the page.
          </p>
        </div>
      </div>
    </section>

    <section class="mb-24">
      <h2 class="text-3xl font-bold text-white mb-8 flex items-center gap-3">
        <span class="text-purple-400" set:html={icons.layers} />
        Layout Composition
      </h2>

      <p class="text-slate-400 mb-8">
        The real power comes from composing layouts. Think of it like nesting
        dolls: a <code>BlogLayout</code> inside a <code>BaseLayout</code>.
      </p>
    </section>

    <section class="text-center py-12 border-t border-slate-800">
      <h3 class="text-2xl font-bold text-white mb-4">Ready to build?</h3>
      <p class="text-slate-400 mb-8">
        Now that you understand the theory, take a look at how this is
        implemented on this very site.
      </p>
    </section>
  </div>
</DocsLayout>
